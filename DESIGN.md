---
name: Showcase
description: 料紙 — 界線を敷いた紙に名乗りと作品が載る。触れた行だけが濡れる
colors:
  paper: "#f6f3eb"
  sumi: "#1c1a15"
  sumi-2: "#3a3730"
  sumi-3: "#55514a"
  sumi-4: "#6a665d"
  kasure: "#8e8a80"
  shu: "#a63a2e"
  aozumi: "#8ba0c2"
  work-paper: "#f6f3ed"
  work-ai: "#224372"
  work-sumi: "#14171b"
typography:
  large:
    fontFamily: "Hina Mincho, Hiragino Mincho ProN, Yu Mincho, serif"
    fontSize: "clamp(1.75rem, 4.2vw, 3rem)"
    fontWeight: 400
    lineHeight: 1.15
    letterSpacing: "0.06em"
  wordmark:
    fontFamily: "Hina Mincho, Hiragino Mincho ProN, Yu Mincho, serif"
    fontSize: "clamp(2.85rem, 12.6vw, 8.75rem)"
    fontWeight: 400
    lineHeight: 0.94
    letterSpacing: "0.075em"
  small:
    fontFamily: "Shippori Mincho, Hiragino Mincho ProN, Yu Mincho, serif"
    fontSize: "clamp(0.8125rem, 0.75rem + 0.22vw, 0.9375rem)"
    fontWeight: 400
    lineHeight: 2.2
    letterSpacing: "0.06em"
  label:
    fontFamily: "Shippori Mincho, Hiragino Mincho ProN, Yu Mincho, serif"
    fontSize: "clamp(0.8125rem, 0.75rem + 0.22vw, 0.9375rem)"
    fontWeight: 400
    lineHeight: 2
    letterSpacing: "0.3em"
rounded:
  none: "0px"
spacing:
  pitch: "calc(2.3 * {typography.small.fontSize})"
  void: "clamp(88px, 40svh, 380px)"
  void-lg: "clamp(112px, 52svh, 500px)"
components:
  seal:
    backgroundColor: "{colors.shu}"
    textColor: "{colors.paper}"
    rounded: "{rounded.none}"
  rule:
    color: "{colors.sumi}"
    opacity: 0.3
  rule-wet:
    color: "{colors.sumi}"
    opacity: 0.85
  choice-mark-on:
    backgroundColor: "{colors.shu}"
---

# Design System: Showcase

## Overview

**Creative North Star: 「料紙」(Ryōshi — the prepared sheet)**

写経の料紙に界線が引かれている。そこへ筆脈が一本、紙を縦に降りる。作品の一行に触れると、その行の紙が濡れ、水が届いた範囲の界線だけが墨を吸って濃くなる。

サイトは一つの世界で通す。トップも作品ページも同じ紙で、違うのは**役の配り方**だけ。

| 面                       | 主役 | 紙の役                             |
| ------------------------ | ---- | ---------------------------------- |
| トップ `/`               | 紙   | 索引そのもの。触れた行が濡れる     |
| 作品ページ `/projects/*` | 作品 | 床の間に退き、作品を載せる台になる |

**Key Characteristics:**

- 枠・角丸・背景色の切り替え・チップ・カードを一つも使わない。構造は縦の余白と墨の濃淡だけが作る
- 字の寸法は「大」と「小」の二段しかない。20〜30px の中間帯を一箇所も使わない
- 線は必ず手の線。印刷の罫（CSS の `border`）ではなく、揺れて・端が沈む墨の線
- 一度の接触で動くものは一つだけ

## 役は三つ。重ならない

| 役                   | 何が担うか | ホバーで       |
| -------------------- | ---------- | -------------- |
| **紙**（最下層）     | 界線       | 変わらない     |
| **構造**（中層）     | 筆脈       | 変わらない     |
| **出来事**（最上層） | 流れ（水） | これだけが動く |

静止の罫（`.work::before`）にホバー規則は一つも無い。触れて現れる濃い罫（`.work::after`）は、形も位置も時間も水のマスクが決める ── **罫はホバーの主語ではなく目的語**である。筆脈も同じで、触れても 1px も動かない。

作品ページには筆脈と流れを持ち込まない。どちらも「段から段へ移る」「索引の行に触れる」ための仕掛けで、あの面には段が一つしか無いから。

## Colors

墨の濃淡だけで語る。色は二箇所にしか無い。

### 紙と墨

- **紙** (#F6F3EB): 地。ここが八割以上を占める
- **墨** (#1C1A15 15.5:1 / #3A3730 10.9:1 / #55514A 7.1:1 / #6A665D 5.2:1): 濃さの四段。字はこの四つだけを使う
- **掠れ** (#8E8A80 3.1:1): 図版専用。**文字色に使わない**

### 朱

- **朱** (#A63A2E, 紙上 5.9:1): トップでは落款ひとつ。作品ページでは戻りの導線を兼ねる
- Operate の面（見積もりシミュレーター）でだけ、朱に役が一つ増える ── **朱は選ばれていることの印**。単価方式の選択とオプションのチェックにだけ点く。朱入れも捺印も、もともと「決めた」ことを示す色だった。**墨は書かれたもの、朱は決めたもの**
- ＊ 朱を「強調」や「注意」に使い始めるとこの理屈は壊れる。選択以外に使わないこと

### 青墨（水にだけ）

- **青墨** (インク #8BA0C2): 藍を含んだ墨。水にだけ乗る
- 合成後に青が出る条件は `−11(1−a) + (ink_B − ink_R)·a > 0` ── 紙は R が B より 11 高い暖色で、水はそれを暗くする層だから。実効不透明度 **a > 0.17** が要る
- 結果、**濃い芯だけが青く、薄い裾は灰のまま**。濃い墨は色を見せ、薄めた墨は灰に見えるという、実際の墨と同じ挙動

### 作品の中の色（紙の色ではない）

- 作品「墨流し」の水盤は **紙 #F6F3ED / 藍 #224372 / 墨 #14171B**
- これは**作品の色**であって紙の色ではない。だから料紙に寄せていない ── 額装が変わっても、掛かっている絵は変わらない

### Named Rules

**The Two Steps Rule.** 字の寸法は「大」(28〜140px) と「小」(13〜15px) の二段だけ。中間帯を作った瞬間に、この世界の呼吸が消える。落款だけは字ではなく図像なので規則の外。

**The Hand Rule.** 線は手の線。`border` で引いた印刷の罫を置かない。界線は feTurbulence を feDisplacementMap に食わせて微かに波打たせ、左を濃く・右へ掠れさせる。

**The One Event Rule.** 一度の接触で動くものは一つ。二つ動いたら、それは役を決めていないということ。

## Layout

- 構造は**縦の余白のリズム**だけが作る。区切り線もカードも使わない
- **起点の斜行** ── 名乗り 1 列目 / 作品 6 列目 / 結び 1 列目。界線の左端が段ごとに右へ階段を上ることで、この斜行が初めて目に見える
- **沈黙の段差** ── 段と段の間は 40〜52svh
- 行送り・余白・落款の寸法は**すべて界線の間隔（pitch）の整数倍**に揃える。字が罫に乗らない料紙は、ただの罫線紙

## 破ってはいけない数値

実測でしか出なかったもの。数値を触るときは測り直すこと。

- **紙が八割。** 画面の八割以上を何も無い紙のまま残す。判定は紙より `Δ≥2/255` 暗い画素を墨として数える最厳の閾値。トップは幅 6 種 × 背丈 2 種 × 状態 3 種の全条件で 81% 以上（最悪 1440×568 のホバー）
- **`--hi-wet-y` は pitch の整数倍。** 半端にすると濡れた罫の位相が静止の罫からずれ、**一本が二本に割れて見える**
- **板の高さは画面の高さで刻む。** 同じ 2 行でも 900px の窓では画面の 15%、568px では 24% を占める。幅をいくら締めてもこの比は動かない。背丈で三段（3 行 / 2 行 / 1 行）、狭くて背が低い窓だけ 0 行
- **濃さと範囲は交換できる。** 濃度を上げても張り出しを締めれば紙はむしろ増える（実測：1440×568 で 80.6% → 83.5%、同時に濃芯の画素は 2.4 倍）。広く薄く撒くより、狭く濃いほうが「濡れた」と読める
- **墨の素材の倍率は低すぎても高すぎてもいけない。** 低いと斑が原寸で並んで「しみ」に見え、高いと斑の内側の均一に濃いところだけを覗くことになって、斑と斑の間の「紙が透ける隙間」ごと消える。連続性は隙間と引き換えに買っている
- **`.stroke` のラッパに `z-index` も `transform` も置かない。** 置いた瞬間そこがスタッキングコンテキストになり、子の `mix-blend-mode: darken` が隔離されて紙と混ざらなくなる（矩形が紙より明るく浮く）

## Components

### 界線（rule）

紙の性格。静止時は墨の alpha 0.30 で、右へ掠れて消える。左端 150 user unit だけ第二の勾配で太い筆致を重ね、書き始めの墨溜まりにする。罫は各行の 0.88 行目に来るので、字は罫の上に座り、y や p の下がりは跨がない。

**大字の帯には引かない。** 罫の間隔は小の明朝に合わせて決まるので、130px 超の大字はどう置いても数本を跨ぎ、「罫線ノートに書いた見出し」になる。

### 筆脈（vein）

段をまたいで降りる一本。段は別々の grid item なので、区間ごとに `viewBox="0 0 100 100"` / `preserveAspectRatio="none"` の箱を置き、終点 x と次の始点 x を揃えて繋ぐ。作品が増えたときの横送りは `src/lib/vein.ts` の `veinPlan()` が計算する ── **`d` を書き足す必要はない**。

三筋の `d` は僅かに違う道を通る。ずれは広い紙で 0.35 単位（約 1.7px）── これ以上離すと三本に割れ、詰めすぎると均一な罫になる。

### 流れ（wet）

触れた行の紙が濡れる。水は自分の面を持つと同時に、**届いた範囲の界線を滲ませて太らせる**。形は罫が決めるのではなく水の雲マスクが決め、時間も水の 1.55s / 0.84s に従う。波形・位相・左右の勾配は静止の罫と完全に同一なので、二重線にならず「同じ罫が滲んで太る」に見える。

### 落款（seal）

朱の小さな正方形。**界線に乗らない寸法**を守る ── 高さ 0.62 行・上寄せ 0.06 行で、上下の罫との間に 5〜7px 残る。罫は feDisplacementMap で ±1.5px 揺れるので、この余裕が要る。

### 動詞（水盤の操作）

ボタンの箱を持たない。紙に書かれた動詞が並んでいるだけで、静止時は界線と同じ淡い一本を敷き、触れると墨を得る。箱が無いので狭い紙では痩せずにそのまま折り返る。指で押す下限 44px は `min-height` で確保する。

### 記入欄（number field）

枠を持たない。数字の下に一本引くだけで、記入する場所を示す ── 帳面の罫と同じ理屈。数字だけが「大」の帯に立つ。フォーカスは輪を外に出さず、下の一本が太くなることで示す（枠の無い世界で輪だけが浮くと、そこだけ別の文法になる）。

## Do's and Don'ts

### Do:

- **Do** 縦の余白で構造を作る。行送り・余白・落款はすべて界線の間隔の整数倍に揃える
- **Do** 線を引くときは手の線にする。揺らぎと、端の沈みを必ず持たせる
- **Do** 一度の接触で動くものを一つに絞る。二つ動くなら役を決め直す
- **Do** 墨を増やしたら実測する。紙が八割を割っていないか、幅と背丈の両方で測る
- **Do** 作品自体の色は作品のものとして残す。額装に合わせて絵を塗り替えない

### Don't:

- **Don't** 角丸・ドロップシャドウ・グラデーション・カード・チップを使わない
- **Don't** 20〜30px の字を作らない（大と小の二段しかない）
- **Don't** `#8E8A80` を文字色に使わない（紙上 3.1:1）
- **Don't** 朱を強調や注意に使わない。朱は「決めた」ことの印にだけ使う
- **Don't** `border` で罫を引かない。それは印刷の罫であって墨ではない
- **Don't** OG 画像を意匠の写しで作らない。実画面を焼く（＊ トップを変えたら焼き直すこと）
